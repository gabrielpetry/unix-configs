echo
cat "$HOME/.aliases" 2>/dev/null
echo $newline
cat "$HOME/.functions" 2>/dev/null
echo $newline
cat "$HOME/petryfiles/zsh-plugins/autosuggestions.zsh" 2>/dev/null
echo $newline
cat "$HOME/petryfiles/zsh-plugins/lib.functions.zsh" 2>/dev/null
echo $newline
# load private stuff
cat "$HOME/Protected/zsh/.aliases" 2>/dev/null
echo $newline
cat "$HOME/Protected/zsh/.functions" 2>/dev/null
